# 般若伴读 - 部署检查清单

本文档提供完整的部署检查清单，确保所有服务正常运行。

## 📋 部署前准备

### 服务器环境

- [ ] 服务器已安装Docker
- [ ] 服务器已安装Docker Compose
- [ ] 服务器已配置好域名DNS解析
  - [ ] `tts.zhdnpx.cn` -> 服务器IP
  - [ ] `files.zhdnpx.cn` -> 服务器IP
- [ ] SSL证书已准备好
  - [ ] `/certs/tts.zhdnpx.cn.crt`
  - [ ] `/certs/tts.zhdnpx.cn.key`
  - [ ] `/certs/files.zhdnpx.cn.crt`
  - [ ] `/certs/files.zhdnpx.cn.key`

### 网络配置

- [ ] Docker网络 `web` 已创建
  ```bash
  docker network create web
  ```

### 目录结构

- [ ] `/opt/nginx/conf.d/` - nginx配置目录
- [ ] `/opt/nginx/prajna/` - App下载页面目录
- [ ] `/opt/hysteria2/certs/` - SSL证书目录
- [ ] TTS服务器代码目录（如 `/root/tts-server/`）

## 🚀 部署步骤

### 1. TTS服务器部署

- [ ] 上传TTS服务器代码到服务器
- [ ] 创建必要的目录
  ```bash
  mkdir -p cache data
  ```
- [ ] 复制 `sections.json` 到 `data/` 目录
- [ ] 构建Docker镜像
  ```bash
  docker build -t prajna-tts-server:latest .
  ```
- [ ] 启动TTS容器
  ```bash
  docker run -d \
    --name prajna-tts-server \
    --restart unless-stopped \
    --network web \
    -p 8000:8000 \
    -v $(pwd)/cache:/app/cache \
    -v $(pwd)/data:/app/data:ro \
    -e HTTPS_PROXY=http://proxy.example.com:7890 \
    prajna-tts-server:latest
  ```
- [ ] 验证TTS服务运行
  ```bash
  docker logs prajna-tts-server
  curl http://localhost:8000/health
  ```

### 2. App下载页面部署

- [ ] 创建页面目录
  ```bash
  sudo mkdir -p /opt/nginx/prajna
  ```
- [ ] 上传页面文件
  ```bash
  scp tts-server/public/index.html user@server:/opt/nginx/prajna/
  ```
- [ ] 设置权限
  ```bash
  sudo chown -R nginx:nginx /opt/nginx/prajna
  sudo chmod 755 /opt/nginx/prajna
  ```
- [ ] （可选）上传截图
  ```bash
  sudo mkdir -p /opt/nginx/prajna/static/images
  scp screenshots/*.png user@server:/opt/nginx/prajna/static/images/
  ```

### 3. Nginx配置

- [ ] 上传nginx配置文件
  ```bash
  scp nginx-config/tts.zhdnpx.cn.conf user@server:/opt/nginx/conf.d/
  scp nginx-config/files.zhdnpx.cn.conf user@server:/opt/nginx/conf.d/
  ```
- [ ] 更新nginx容器（如果需要添加挂载）
  ```bash
  docker stop sub-nginx
  docker rm sub-nginx
  
  docker run -d \
    --name sub-nginx \
    --restart unless-stopped \
    --network web \
    -p 80:80/tcp \
    -p 443:443/tcp \
    -v /opt/nginx/nginx.conf:/etc/nginx/nginx.conf:ro \
    -v /opt/nginx/conf.d:/etc/nginx/conf.d:ro \
    -v /opt/nginx/prajna:/usr/share/nginx/prajna:ro \
    -v /opt/subscription/www:/usr/share/nginx/html:ro \
    -v /opt/hysteria2/certs:/certs:ro \
    nginx:alpine
  ```
- [ ] 测试nginx配置
  ```bash
  docker exec sub-nginx nginx -t
  ```
- [ ] 重新加载nginx
  ```bash
  docker exec sub-nginx nginx -s reload
  ```

### 4. GitHub Release配置

- [ ] 在GitHub上创建Release
- [ ] 上传APK文件（Prajna-Reader.apk）
- [ ] 设置版本号（如 v1.0.0）
- [ ] 填写更新说明
- [ ] 发布Release
- [ ] 验证下载链接
  ```
  https://github.com/mafeicnnui/PrajnaReader/releases/latest/download/Prajna-Reader.apk
  ```

## ✅ 功能测试

### TTS API测试

- [ ] 健康检查
  ```bash
  curl https://tts.zhdnpx.cn/health
  # 预期: {"ok":true}
  ```
- [ ] 获取sections列表
  ```bash
  curl https://tts.zhdnpx.cn/sections
  ```
- [ ] 生成音频（男声）
  ```bash
  curl -I "https://tts.zhdnpx.cn/tts/section/mp3?section_id=preface-1&voice=male"
  # 预期: HTTP/1.1 200 OK
  ```
- [ ] 生成音频（女声）
  ```bash
  curl -I "https://tts.zhdnpx.cn/tts/section/mp3?section_id=preface-1&voice=female"
  # 预期: HTTP/1.1 200 OK
  ```
- [ ] 测试CORS
  ```bash
  curl -H "Origin: https://example.com" \
       -H "Access-Control-Request-Method: GET" \
       -X OPTIONS https://tts.zhdnpx.cn/tts/section/mp3
  # 预期: 包含 Access-Control-Allow-Origin 头
  ```

### App下载页面测试

- [ ] 访问主页
  ```
  https://tts.zhdnpx.cn/
  ```
  - [ ] 页面正常显示
  - [ ] 样式正确加载
  - [ ] 二维码正常显示
- [ ] 手机访问测试
  - [ ] 页面响应式布局正常
  - [ ] 可以正常滚动
  - [ ] 按钮可以点击
- [ ] 下载功能测试
  - [ ] 点击下载按钮
  - [ ] 跳转到GitHub Release页面
  - [ ] 可以下载APK文件
- [ ] 二维码测试
  - [ ] 使用手机扫描二维码
  - [ ] 跳转到下载页面
  - [ ] 可以下载APK

### 前端App测试

- [ ] 启动前端开发服务器
  ```bash
  cd prajna-buddy
  npm run dev
  ```
- [ ] 访问 `http://localhost:5173`
- [ ] 测试播放功能
  - [ ] 选择一个章节
  - [ ] 点击段落播放按钮
  - [ ] 检查浏览器控制台
    - [ ] 应该看到 `[TTS API] 音频URL: https://tts.zhdnpx.cn/...`
    - [ ] 音频应该正常播放
  - [ ] 切换男声/女声
    - [ ] 应该重新加载音频
    - [ ] 控制台显示新的URL
  - [ ] 测试连续播放
  - [ ] 测试单段循环

### 缓存测试

- [ ] 首次播放某段音频
  - [ ] 检查TTS服务器日志
  - [ ] 应该看到生成音频的日志
- [ ] 再次播放同一段音频
  - [ ] 检查TTS服务器日志
  - [ ] 应该看到从缓存读取的日志
- [ ] 检查浏览器缓存
  - [ ] 打开浏览器开发者工具 -> Network
  - [ ] 重新播放已播放过的音频
  - [ ] 应该看到 `(from disk cache)` 或 `304 Not Modified`

## 🔍 监控和日志

### 查看日志

- [ ] TTS服务器日志
  ```bash
  docker logs -f prajna-tts-server
  ```
- [ ] Nginx日志
  ```bash
  docker logs -f sub-nginx
  ```
- [ ] 查看特定请求
  ```bash
  # 查看TTS API请求
  docker logs sub-nginx 2>&1 | grep "/tts/"
  
  # 查看页面访问
  docker logs sub-nginx 2>&1 | grep "GET / "
  ```

### 性能监控

- [ ] 检查TTS服务器资源使用
  ```bash
  docker stats prajna-tts-server
  ```
- [ ] 检查缓存目录大小
  ```bash
  du -sh cache/
  ```
- [ ] 检查音频生成时间
  - [ ] 查看TTS日志中的时间戳
  - [ ] 首次生成应该在1-3秒
  - [ ] 缓存命中应该在100ms以内

## 🐛 故障排查

### TTS服务无法访问

- [ ] 检查容器是否运行
  ```bash
  docker ps | grep prajna-tts-server
  ```
- [ ] 检查容器日志
  ```bash
  docker logs prajna-tts-server
  ```
- [ ] 检查网络连接
  ```bash
  docker exec sub-nginx curl http://prajna-tts-server:8000/health
  ```
- [ ] 检查防火墙规则

### 页面无法访问

- [ ] 检查nginx容器是否运行
  ```bash
  docker ps | grep sub-nginx
  ```
- [ ] 检查文件是否存在
  ```bash
  docker exec sub-nginx ls -l /usr/share/nginx/prajna/
  ```
- [ ] 检查nginx配置
  ```bash
  docker exec sub-nginx nginx -t
  ```
- [ ] 检查nginx日志
  ```bash
  docker logs sub-nginx | tail -50
  ```

### 音频播放失败

- [ ] 检查浏览器控制台错误
- [ ] 检查网络请求
  - [ ] 打开开发者工具 -> Network
  - [ ] 查看失败的请求
  - [ ] 检查状态码和响应
- [ ] 检查TTS服务器日志
  ```bash
  docker logs prajna-tts-server | grep ERROR
  ```
- [ ] 测试直接访问API
  ```bash
  curl -v "https://tts.zhdnpx.cn/tts/section/mp3?section_id=preface-1&voice=male"
  ```

### CORS错误

- [ ] 检查nginx配置中的CORS头
- [ ] 检查响应头
  ```bash
  curl -I -H "Origin: https://example.com" https://tts.zhdnpx.cn/health
  ```
- [ ] 确保所有location块都包含CORS配置
- [ ] 重新加载nginx配置

## 📊 性能优化

### 已实施的优化

- [x] TTS服务器缓存（文件系统）
- [x] 浏览器缓存（Cache-Control头）
- [x] Nginx keepalive连接
- [x] HTTP/2支持
- [x] Gzip压缩（如果启用）

### 可选优化

- [ ] 添加CDN加速（Cloudflare等）
- [ ] 启用nginx缓存
- [ ] 预生成常用音频
- [ ] 添加Redis缓存层
- [ ] 使用更快的TTS引擎

## 🔐 安全检查

- [ ] SSL证书有效期检查
  ```bash
  echo | openssl s_client -connect tts.zhdnpx.cn:443 2>/dev/null | openssl x509 -noout -dates
  ```
- [ ] 检查HSTS头
  ```bash
  curl -I https://tts.zhdnpx.cn/ | grep Strict-Transport-Security
  ```
- [ ] 检查隐藏文件访问
  ```bash
  curl -I https://tts.zhdnpx.cn/.env
  # 预期: 403 Forbidden
  ```
- [ ] 检查敏感文件访问
  ```bash
  curl -I https://tts.zhdnpx.cn/data/sections.json
  # 预期: 404 Not Found（不应该直接访问）
  ```

## 📝 部署完成确认

完成以上所有检查后，在下方签名确认：

- 部署人员：__________
- 部署日期：__________
- 版本号：v1.0.0
- 备注：__________

## 🔗 相关文档

- [TTS服务器部署说明](./TTS服务器部署说明.md)
- [App下载页面部署说明](./App下载页面部署说明.md)
- [音频系统架构说明](./音频系统架构说明.md)
- [前端播放器更新说明](./前端播放器更新说明.md)

---

最后更新：2026年3月26日
